package com.keepsafe.notes.models;

public enum AppRole {
    //This enum has the list of roles for our users that is available in our application. These roles are assigned to the user to determine their authorities and priveleges.

    ROLE_USER,
    ROLE_ADMIN
}
